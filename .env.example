# Production Environment Variables Template for Vercel

# 1. Database (PostgreSQL)
# DATABASE_URL: Transaction Pooler (Session/Transaction mode - Port 6543)
# Use this for the main connection in Vercel to avoid connection exhausted errors.
DATABASE_URL="postgresql://postgres:[PASSWORD]@db.[PROJECT-REF].supabase.co:6543/postgres?pgbouncer=true&connection_limit=1&pg_prepared_statements=false"

# DIRECT_URL: Direct Connection (Port 5432)
# Prisma uses this for migrations (db push) while using the pooler for queries.
DIRECT_URL="postgresql://postgres:[PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres"

# 2. Authentication (NextAuth)
# Generate a secret with: openssl rand -base64 32
AUTH_SECRET="your-secret-here"
AUTH_URL="https://your-domain.vercel.app"

# 3. Cloud Storage (Option 1: Supabase Storage)
# SUPABASE_URL="your-supabase-url"
# SUPABASE_ANON_KEY="your-anon-key"
# SUPABASE_BUCKET="prompts"

# 4. Cloud Storage (Option 2: Cloudinary) - Easier for images
CLOUDINARY_CLOUD_NAME="your-cloud-name"
CLOUDINARY_API_KEY="your-api-key"
CLOUDINARY_API_SECRET="your-api-secret"
